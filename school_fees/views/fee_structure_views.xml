<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- list (List) View -->
    <record id="view_school_fee_structure_list" model="ir.ui.view">
        <field name="name">school.fee.structure.list</field>
        <field name="model">school.fee.structure</field>
        <field name="arch" type="xml">
            <list string="Fee Structures">
                <field name="name"/>
                <field name="grade_id"/>
                <field name="academic_year_id"/>
                <field name="term"/>
                <field name="total_amount" sum="Total" widget="monetary"/>
                <field name="company_id" groups="base.group_multi_company" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_school_fee_structure_form" model="ir.ui.view">
        <field name="name">school.fee.structure.form</field>
        <field name="model">school.fee.structure</field>
        <field name="arch" type="xml">
            <form string="Fee Structure">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Grade 10 - Standard Fees"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Target Category">
                            <field name="grade_id" options="{'no_create': True}"/>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        </group>
                        <group string="Academic Period">
                            <field name="academic_year_id" options="{'no_create': True}"/>
                            <field name="term"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Fee Components" name="lines">
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="product_id" options="{'no_create': True}" domain="[('sale_ok', '=', True)]"/>
                                    <field name="amount" sum="Subtotal"/>
                                    <field name="currency_id" column_invisible="True"/>
                                </list>
                            </field>
                            <group class="oe_subtotal_footer oe_right">
                                <field name="total_amount" class="oe_subtotal_footer_separator" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            </group>
                            <div class="clearfix"/>
                        </page>
                    </notebook>
                </sheet>
                <!-- Chatter for mail.thread and mail.activity.mixin -->
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_school_fee_structure_search" model="ir.ui.view">
        <field name="name">school.fee.structure.search</field>
        <field name="model">school.fee.structure</field>
        <field name="arch" type="xml">
            <search string="Search Fee Structures">
                <field name="name"/>
                <field name="grade_id"/>
                <field name="academic_year_id"/>
                <group expand="0" string="Group By">
                    <filter string="Grade" name="group_by_grade" context="{'group_by': 'grade_id'}"/>
                    <filter string="Academic Year" name="group_by_year" context="{'group_by': 'academic_year_id'}"/>
                    <filter string="Term" name="group_by_term" context="{'group_by': 'term'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_school_fee_structure" model="ir.actions.act_window">
        <field name="name">Fee Structures</field>
        <field name="res_model">school.fee.structure</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Fee Structure!
            </p>
            <p>
                Define different fee sets for each grade and academic term.
            </p>
        </field>
    </record>

    <menuitem id="menu_fee_structures"
              name="Fee Structures"
              parent="school_fees.menu_school_fees_config"
              action="action_school_fee_structure"
              sequence="10"/>

</odoo>
